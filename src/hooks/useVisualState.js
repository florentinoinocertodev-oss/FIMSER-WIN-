/**
 * Hook shim
 * Re-export useVisualState from the new contexts location so components can adopt hooks path patterns.
 */

export { useVisualState } from "../contexts/VisualStateProvider.jsx";
export { VisualStateProvider } from "../contexts/VisualStateProvider.jsx";